# SparkRDD_wordcount
Explore RDD structures 
In the colab notebook, we try to import pyspark and create a RDD by textFile() and map it to a Key-value RDD.
A few transformation functions including reduceByKey, mapValues, filter and action function, collect are applied in colab notebook to perform a word count application for any text file.
